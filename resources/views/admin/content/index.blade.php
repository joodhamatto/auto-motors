@extends('layouts.admin') @section('title',$title) @section('content')
<div class="admin-panel"><div class="panel-toolbar"><form class="search-form"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Search…"><button class="btn btn-outline-secondary"><i class="bi bi-search"></i></button></form><a class="btn btn-brand" href="{{ route('admin.content.create',$resource) }}"><i class="bi bi-plus-lg"></i> Add new</a></div><div class="table-responsive"><table class="table align-middle"><thead><tr><th>ID</th>@foreach($columns as $column)<th>{{ str($column)->replace('_',' ')->title() }}</th>@endforeach<th>Order</th><th>Status</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->id }}</td>@foreach($columns as $column)<td>{{ str($item->{$column})->limit(60) }}</td>@endforeach<td>{{ $item->display_order }}</td><td><span class="badge {{ $item->is_active?'text-bg-success':'text-bg-secondary' }}">{{ $item->is_active?'Active':'Inactive' }}</span></td><td><div class="table-actions"><form method="post" action="{{ route('admin.content.toggle',[$resource,$item->id]) }}">@csrf<button class="btn btn-sm btn-outline-secondary" title="Toggle status"><i class="bi bi-power"></i></button></form><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.content.edit',[$resource,$item->id]) }}"><i class="bi bi-pencil"></i></a><form method="post" action="{{ route('admin.content.destroy',[$resource,$item->id]) }}" data-confirm="Delete this record permanently?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="{{ count($columns)+4 }}"><div class="admin-empty"><i class="bi bi-inbox"></i><p>No records found.</p></div></td></tr>@endforelse</tbody></table></div>{{ $items->links() }}</div>
@endsection
